{
  "dependencies": {
    "serve": "^14.2.6",
    "wikibase-dump-filter": "^6.1.1"
  },
  "scripts": {
    "dev": "serve dist",
    "start": "cp dist/* $TOOL_DATA_DIR/dist/ && serve $TOOL_DATA_DIR/dist"
  }
}
